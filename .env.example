# Flask runtime (required)
SECRET_KEY=
SESSION_COOKIE_SECURE=false

# Optional Redis cache/session store. The app falls back to local storage.
REDIS_URL=redis://localhost:6379

# Optional Google Cloud Storage integration.
GCS_BUCKET=
GOOGLE_APPLICATION_CREDENTIALS=

# Optional summarization providers.
HF_TOKEN=
HF_API_TOKEN=
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=
RUNPOD_VLLM_BASE_URL=
OPENAI_API_KEY=

# Install this file for the local/RunPod/GCP summarization pipelines.
# The Flask catalog application only needs requirements.txt.
-r requirements.txt

accelerate==1.13.0
bitsandbytes==0.49.2
huggingface-hub==1.20.1
ollama==0.6.2
sentencepiece==0.2.1
torch==2.12.1
transformers==5.12.1

# flash-attn is intentionally optional. summarizer.py detects it at runtime and
# falls back to PyTorch SDPA on hardware where flash-attn is unavailable.
